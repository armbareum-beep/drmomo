# 수의학 RAG 시스템

PDF 문서 기반 수의학 질의응답 AI 시스템입니다.

## 기능

- 📚 PDF, DOCX, TXT 등 다양한 문서 형식 지원
- 🤖 OpenAI GPT-4 기반 정확한 답변
- 🌐 웹 인터페이스 제공
- 🎨 모던한 다크 모드 UI
- ⚡ 실시간 채팅 인터페이스

## 설치 방법

1. **의존성 설치**
```bash
pip install -r requirements.txt
```

2. **환경 변수 설정**
`.env.example` 파일을 `.env`로 복사하고 OpenAI API 키를 입력하세요.

3. **문서 추가**
`documents` 폴더에 수의학 관련 PDF 파일을 추가하세요.

## 실행 방법

### 웹 인터페이스 (권장)
```bash
python app.py
```
브라우저에서 `http://localhost:5000` 접속

### 커맨드라인 인터페이스
```bash
python main.py
```

## 프로젝트 구조

```
.
├── app.py                 # Flask 웹 애플리케이션
├── main.py               # CLI 인터페이스
├── rag_system.py         # RAG 시스템 코어 로직
├── create_dummy_pdf.py   # 테스트용 PDF 생성
├── templates/            # HTML 템플릿
│   └── index.html
├── static/              # CSS, JS 파일
│   └── style.css
├── documents/           # 학습할 문서 폴더
└── requirements.txt     # 의존성 목록
```

## 주의사항

⚠️ 이 시스템의 답변은 참고용이며, 실제 진단·처방은 반드시 수의사가 직접 판단해야 합니다.

## 라이선스

MIT License
